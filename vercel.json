{
  "buildCommand": "npm install && npm run build && pip install -r requirements.txt && python manage.py collectstatic --noinput --settings=config.settings.production",
  "functions": {
    "api/index.py": {
      "runtime": "python3.12",
      "includeFiles": "staticfiles/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "api/index.py" }
  ],
  "headers": [
    {
      "source": "/static/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
