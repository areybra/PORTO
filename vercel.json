{
  "version": 2,
  "buildCommand": "npm install && npm run build && python manage.py collectstatic --noinput --settings=config.settings.production",
  "builds": [
    { "src": "api/index.py", "use": "@vercel/python", "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
